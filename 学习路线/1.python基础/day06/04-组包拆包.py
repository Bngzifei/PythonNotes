"""
组包/包装:return的时候 *args 的时候 % 格式化的时候  组包默认都是元组,多个值赋值给一个值时,组包只能组成元组
解包/拆包:容器类型的数据都可以拆包,但是有个条件:等号左边的变量个数一定要和等号右边的容器内元素个数一致(相等).
"""

a = 10, 20  # 如果多个值赋值一个变量时,会自动组包为元组(默认组的都是元组)

tuple1 = (10, 20)
list1 = [11, 23]
str1 = "12"
dict1 = {'n': 111, 'm': 222}

# m, d = tuple1
# m, d = list1
# m, d = str1
# m, d = dict1  # key 拆包出来的默认是key
m, d = dict1.values()  # 需要value的时候这么写
print(m, d, sep='-----------')  # sep: str = ' ' ----> 关键字参数
